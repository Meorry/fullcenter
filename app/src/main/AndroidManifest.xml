<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.ucai.fullcenter">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FuLiCenterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.SpflashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.GoodsDetailsActivit" />
        <activity android:name=".activity.BoutiqueSecondActivity" />
        <activity android:name=".activity.CategorySecondActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.PersonalCenterActivity" />
        <activity android:name=".activity.UpdateUserNickActivity" />
        <activity android:name=".activity.CollectGoodsActivity"></activity>
    </application>

</manifest>